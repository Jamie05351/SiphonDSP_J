<?xml version="1.0" encoding="utf-8"?>
<!-- See fragment_native_bmw_compressor.xml (portrait) for the full rationale. Landscape's per-
     band page (layout-land/page_compressor_band.xml) has its own left/right split built in, so
     this fragment-level layout is just the full-bleed pager, same as portrait. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/compressor_slider_pager_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent" />
