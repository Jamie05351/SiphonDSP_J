<?xml version="1.0" encoding="utf-8"?>
<!-- A pinned CompressorSurface (pre-crossover multiband-compressor visualiser: live spectrum,
     the four crossover-band regions, per-band threshold lines and the applied gain-reduction
     curve) sits above the band pager. Each band's page (page_compressor_band.xml) is still
     inflated twice by NativeBmwCompressorFragment and paged via DspPager below. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical">

    <com.google.android.material.card.MaterialCardView
        android:id="@+id/compressor_surface_card"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="8dp"
        app:cardCornerRadius="20dp">

        <app.siphondsp.view.CompressorSurface
            android:id="@+id/compressor_surface"
            android:layout_width="match_parent"
            android:layout_height="200dp" />
    </com.google.android.material.card.MaterialCardView>

    <FrameLayout
        android:id="@+id/compressor_slider_pager_container"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />
</LinearLayout>
